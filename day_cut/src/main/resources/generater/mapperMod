<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="${relativePackge}.mapper.${modelName}Mapper">

    <resultMap id="BaseResultMap" type="${relativePackge}.model.${modelName}">
            <${tagType} property="${property}" column="${column}"/>
    </resultMap>

    <sql id="Base_Column_List">
       ${baseColumn}
    </sql>
</mapper>
#end